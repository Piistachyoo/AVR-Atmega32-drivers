/*
 * main.c
 *
 *  Created on: Jan 29, 2023
 *      Author: omarm
 */
/*
#include "DIO_Interface.h"

int main()
{
	SET_Direction_4LSB(PORTC, INPUT);
	SET_State_4LSB(PORTC, HIGH);
	SET_Direction(PORTD, PIN0, OUTPUT);
	while(1)
	{
		if(GET_State(PORTC, PIN0)==0)
		{
			SET_State(PORTD, PIN0, HIGH);
		}
		else
		{
			SET_State(PORTD, PIN0, LOW);
		}
	}
	return 0;
}
*/